namespace PaymentService.Application.Tests;

// Placeholder — add application-layer tests here as features are implemented.
public class PlaceholderTests
{
    [Fact]
    public void Application_TestInfrastructure_IsOperational()
    {
        // Verifies that the test project compiles and the runner can execute tests.
        Assert.True(true);
    }
}
